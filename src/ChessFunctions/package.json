{
  "name": "chess_functions",
  "version": "1.0.0",
  "description": "c++ chess functions",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npm run build",
    "build": "node-gyp configure build --release --no-debug && tsc --declaration"
  },
  "author": "Surien Das-Giwojno",
  "license": "MIT",
  "dependencies": {
    "bindings": "^1.5.0"
  },
  "devDependencies": {
    "@types/bindings": "^1.3.0",
    "napi_thread_safe_promise": "^1.0.10",
    "node-addon-api": "^3.0.0",
    "typescript": "^3.8.3"
  },
  "files": [
    "/**/*.h",
    "/**/*.hpp",
    "/**/*.js",
    "/**/*.d.ts",
    "/**/*.node"
  ]
}
